#pragma once

#include <iostream>
#include "include/globalbounding.hpp"

#define DIM 3

class MortonKeys{
    private:
    uint32_t* keys;
    int* indices;

    public:
    MortonKeys(double* x, double* y, double* z){
        
    }
};